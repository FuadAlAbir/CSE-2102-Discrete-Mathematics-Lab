#include<stdio.h>

int bitValue(int a)
{
    int bitAr[100], i = 0;
    while(a/2 != 0)
    {
        bitAr[i] = a%2;
        i++;
        a = a / 2;
    }
    for()
}

int main()
{
    bitValue(15);
}
